'''
We need to find the length of the longest subarray that has a sum less than or equal to k. For this example, let nums = [3, 2, 1, 3, 1, 1] and k = 5.
'''
def longestSubArray(nums: list[int]) -> int:

    pass
